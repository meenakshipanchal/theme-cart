{% comment %} 
<div class="rv-horizontal-grid__item">
  {% render 'variant-product',
    card_product: product.selected_or_first_available_variant,
    product: product,
    media_aspect_ratio: 'adapt',
    image_shape: 'default',
    show_secondary_image: false,
    show_vendor: false,
    show_rating: true,
    quick_add: 'standard',
    skip_styles: skip_card_product_styles
  %}
</div>

{% schema %}
{
  "name": "Recently Viewed Card",
  "settings": []
}
{% endschema %}  {% endcomment %}