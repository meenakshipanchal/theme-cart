<style>
  .card__media .flat-discount-badge {
    /* Positioning */
    position: absolute;
    top: 0px;
    right: 0px;
    z-index: 12 !important;
    
    /* Styling */
    background-color: #235a49;
    color: #ffffff;
    padding: 6px 14px;
    font-size: 13px;
    font-weight: 600;
    border-radius: 9999px;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    line-height: 1;

    /* Shadow */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); 

    /* Transition */
    transition: all 0.2s ease-in-out; 
  }

  .card__media .flat-discount-badge:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
  }

  /* === MOBILE VIEW UPDATE === */
  @media (max-width: 768px) {
    .card__media .flat-discount-badge {
      top: 0;
      right: 0;
      padding: 3px 8px; /* Padding aur kam kar di */
      font-size: 9px;   /* Font size aur chhota kar diya */
      border-radius: 0;
      border-bottom-left-radius: 8px; /* Radius bhi thoda adjust kiya */
    }
  }
</style>

  <div class="flat-discount-badge">
    <span>30% Off</span>
  </div>